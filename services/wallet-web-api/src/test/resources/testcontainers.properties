# Reuse containers across test runs for faster execution
testcontainers.reuse.enable=true

# Disable Ryuk (container cleanup service) - it also requires network access
ryuk.disabled=true

# Disable checks
checks.disable=true
