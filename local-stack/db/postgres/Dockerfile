# Latest stable Postgres (alpine variant, matching the CI image style).
# `alpine` tracks the newest stable Postgres release. To pin for
# reproducibility, replace with an explicit tag, e.g. `postgres:17-alpine`.
FROM postgres:alpine
